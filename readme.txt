# 🚦 Real-Time Traffic Prediction Using Machine Learning and IoT Data

This project implements a **real-time traffic prediction system** that uses **machine learning (LSTM)** and simulated **IoT sensor data** to predict traffic volume on multiple city roads. The results are displayed on a live dashboard using **Flask** and **Plotly**.

